# Reusable workflows

Place shared GitHub Actions workflows here as the organization grows.

Future repositories can reference them with:

```yaml
uses: brig-id/.github/.github/workflows/<workflow-file>@main
```
